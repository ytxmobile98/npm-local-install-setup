# NPM local install setup

This repository provides a setup script that allows you to do `npm-install -g` for the current user.

## Setup

Run:
```
$ bash setup.sh
```

## Remove setup

Run:
```
$ bash remove-setup.sh
```

## References

* `$ npm help config`
* `$ npm help folders`
